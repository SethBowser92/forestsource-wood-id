// Optional: Sentry client init
// import * as Sentry from '@sentry/nextjs'
// Sentry.init({ dsn: process.env.SENTRY_DSN, tracesSampleRate: 0.1, environment: process.env.SENTRY_ENV || 'production' })
